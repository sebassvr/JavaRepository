/**
 * @author sebassvr
 */
/**  Hilos en java (Threads)
 * Un hilo o también conocido como Thread por su
 * traducción al inglés, es un flujo de control dentro
 * de un programa, el cual permite tener múltiples
 * procesos corriendo de forma simultánea.
 * Para esto hay dos formas de hacerlo:
 * 1. Heredando: extends Thread { }
 * y sobreescribimos el método run(){ }
 * 2. Implementando la interfaz Runnable: implements Runnable{ }
 * y sobreescribimos el método run(){ }
*/

package hilos_threads;

public class Hilos {

}
